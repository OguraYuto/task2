
<%= flash[:notice] %>

<h2>Title</h2>
  <p><%= @book.title %></p>
<h2>Body</h2>
  <p><%= @book.body %></p>

<%= link_to "Edit", edit_books_path(@book.id) %>
  <a>|</a>
<%= link_to "Back", books_path(@book.id) %>